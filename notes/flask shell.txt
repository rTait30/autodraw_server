flask shell

from models import db, User
admin = User(username='admin', role='admin', verified=True, email='admin@example.com', company='YourCompany')
admin.set_password('your_admin_password')
db.session.add(admin)
db.session.commit()
print("Admin user created!")